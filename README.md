## 🚀 Запуск проекта в режиме разработки (Local Development)

### 1. Клонирование репозитория
```bash
git clone https://github.com/LazyFoxx/flashmind-auth.git
cd auth-service
```
### 2. Создание файла с переменными окружения
Скопируй пример файла и отредактируй при необходимости:

```bash
cp .env.dev.example .env.dev
python -m cli.main  # генерирует пару ключей RS256
```

### 3. Запуск всех сервисов
```bash
docker compose up -d
```

### 4. Основные команды
следишь за логами и hot-reload'ом:
```bash
docker compose logs -f auth-dev ( нужно чтобы отслуживать коды подтверждения в консоли)

# Остановить все контейнеры
docker compose down

# Остановить и удалить volumes (полная очистка данных — осторожно!)
docker compose down -v
```

После запуска будут доступны следующие сервисы:

Swagger UI       http://localhost:8000/docs       | Интерактивная документация API
Redoc            http://localhost:8000/redoc      | Альтернативная документация

#### Как подключиться к базам через GUI

**Adminer (он проще)** (http://localhost:8080)
- System: `PostgreSQL`
- Server: `auth-postgres-dev`
- Username: `user`
- Password: `password`
- Database: `db`

**pgAdmin** (http://localhost:5050)
- Email: `admin@example.com` (или значение из `.env.dev`)
- Password: `supersecretpgadmin`
- Добавить сервер → Host name/address: `postgres-dev`, Port: `5432`, Username/Password как выше
